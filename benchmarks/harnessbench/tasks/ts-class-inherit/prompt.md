`Circle` and `Rectangle` in src/shapes.ts both extend `Shape`. `Shape.describe()` should return `"Shape: <area>"` where `<area>` is the result of `this.area()`. Subclasses override `area()`. But `describe()` currently ignores the subclass's `area()` and always returns the same string. Fix `describe()` so it calls the subclass's `area()` correctly via polymorphism. Do not change `area()` in either subclass.
