The order state machine in src/order.ts allows illegal transitions: an order can currently go shipped -> paid and cancelled -> anything. The legal transitions are exactly: pending -> paid | cancelled; paid -> shipped | refunded; shipped -> delivered; and delivered, cancelled, refunded are terminal. `transition` must throw an Error naming the from/to states for any illegal move and return the new state for legal ones. Fix the table/logic; keep exports.
