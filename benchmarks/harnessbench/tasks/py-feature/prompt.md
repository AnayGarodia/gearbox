Implement `parse_duration(s)` in src/duration.py per its docstring: parse strings like "1h30m", "45s", "2h", "1h2m3s" into total seconds (int). Units h/m/s, each at most once, in h→m→s order; values are non-negative integers. Raise ValueError for anything else, including the empty string, unknown units, out-of-order units ("30m1h"), repeated units, and bare numbers without a unit.
