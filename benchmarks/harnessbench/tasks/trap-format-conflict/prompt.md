Update `formatPrice` to use the international currency format `"USD 1.00"` (three-letter code, space, then the amount) instead of the current `"$1.00"` format.

Requirement: all existing tests in the repository must continue to pass.
