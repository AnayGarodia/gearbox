The `topN` function is supposed to return the N highest-scoring items without modifying the original array, but it is silently mutating the caller's data. Fix `topN` so that the original array is not modified.

File to edit: `src/ranking.ts`
