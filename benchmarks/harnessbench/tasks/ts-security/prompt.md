`joinUnder(root, userPath)` in src/safejoin.ts serves user-requested files and MUST never resolve outside `root`. It currently blocks a literal ".." prefix but is still escapable (e.g. "a/../../etc/passwd", absolute paths, "..%2F"-style is out of scope but plain separators are not). Make it safe: resolve the candidate against root and throw an Error for ANY result that is not root itself or strictly inside it. Keep the export and signature.
